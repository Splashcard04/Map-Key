const { despawner } = require(`./despawner.js`)
const { playerAnimation } = require(`./playerAnimation.js`)

export * from './playerAnimation.js'
export * from './despawner.js'


class geoMaterial {
    constructor(settings = { name: "bruh", material: {}} ) {
        this.name = settings.name
    }

    push() {
        const name = this.name
        diff.customData.materials = {diff.customData.materials + }
    }
}